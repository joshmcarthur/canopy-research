[project]
name = "canopy-research"
version = "0.1.0"
description = "Canopy Research is a Django-based application for managing and analyzing research data."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "django>=5.2.11",
    "django-unfold>=0.79.0",
]
